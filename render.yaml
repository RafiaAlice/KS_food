services:
  - type: web
    name: kansas-food-pantry
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9
    plan: free
